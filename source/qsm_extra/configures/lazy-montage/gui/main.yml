option:
    type: 'dcc-tool-panel'
    # config used system
    systems:
        - '*-maya'
    #
    gui:
        name: 'Lazy Montage'
        name_chs: '懒人拼接'
        group_name: 'Animation(s)'
        #
        icon_name: 'qosmic/lazy-resource-tool'
        tool_tip:
            |-
            For motion composition.
        #
        tool_tip_chs:
            |-
            用于动作拼接。

        size: [960, 720]

build:
    main:
        messages:
            exists_master_layer:
                message_chs: '主层已经存在。'
                message: 'Master layer is exists.'

            no_character:
                message_chs: '选择一个角色（可以是任意部件）。'
                message: 'Select one character (any part).'

    resource:
        tab:
            name: 'Resource'
            name_chs: '资源'

            tool_tip:
                |-
                For motion splicing.

            tool_tip_chs:
                |-
                用于动作拼接。

    splicing:
        tab:
            name: 'Splicing'
            name_chs: '拼接'

            tool_tip:
                |-
                For motion splicing.

            tool_tip_chs:
                |-
                用于动作拼接。
