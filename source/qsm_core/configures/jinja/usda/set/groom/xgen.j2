#usda 1.0
(
    defaultPrim = "{{ dcc.root_name }}"
    metersPerUnit = 0.01
    upAxis = "Y"
)

def "{{ dcc.root_name }}" (
    kind = "component"
)
{
    def "hair" (
        kind = "subcomponent"
    )
    {
    }
}
