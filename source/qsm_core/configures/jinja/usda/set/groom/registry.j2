#usda 1.0
(
    defaultPrim = "{{ dcc.root_name }}"
    metersPerUnit = 0.01
    upAxis = "Y"
)

over "{{ dcc.root_name }}" (
    assetInfo = {
        asset identifier = @{{ asset }}.usda@
        string project = "{{ project }}"
        string role = "{{ role }}"
        string asset = "{{ asset }}"
        string step = "{{ step }}"
        string task = "{{ task }}"
        string version = "{{ version }}"
    }
    kind = "component"
    prepend references = [
        @{{ usdas.xgen }}@,
    ]
)
{
}